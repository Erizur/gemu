{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch lldb",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/gemu",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Compile (debug)",
      "postLaunchTask": "Clean"
    }
  ]
}
